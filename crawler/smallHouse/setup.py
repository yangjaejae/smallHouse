from setuptools import setup, find_packages

setup(
    name = "realNews",
    author = 'undertakin',
    version = "0.1",
    packages = find_packages()
)

setup(
    name = "realestateDeal",
    author = 'undertakin',
    version = "0.1",
    packages = find_packages()
)